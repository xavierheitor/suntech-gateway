services:
  suntech-gateway:
    build: .
    container_name: suntech-gateway
    restart: unless-stopped
    env_file: .env
    ports:
      - "7771:7771"
      - "7777:7777/tcp"
    volumes:
      - gateway-data:/app/data

volumes:
  gateway-data:
